import React from "react";
import { View, Text, Button } from "react-native";

const custombutton = () => {
    return(
        <View>
            
        </View>
    );
};

export default custombutton